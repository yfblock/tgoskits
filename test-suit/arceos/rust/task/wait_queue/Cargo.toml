[package]
name = "arceos-wait-queue"
version = "0.3.1"
edition.workspace = true
authors = ["Keyang Hu <keyang.hu@qq.com>"]
description = "A simple demo to test the wait queue for tasks under ArceOS"
publish = false

[dependencies]
ax-std = { workspace = true, features = ["multitask", "irq"], optional = true }
